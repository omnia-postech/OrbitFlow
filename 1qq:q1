[1mdiff --git a/examples/test_distN.py b/examples/test_distN.py[m
[1mindex a75966cdf..6d6849fd1 100644[m
[1m--- a/examples/test_distN.py[m
[1m+++ b/examples/test_distN.py[m
[36m@@ -625,6 +625,7 @@[m [mdef main(configs):[m
     batch_size = trace.batch_size if hasattr(trace, "batch_size") else BATCH_SIZE[m
     # prompts = trace.requests if hasattr(trace, "requests") else trace.samples[m
     static_batching = configs.static_batching if hasattr(configs, "static_batching") else False[m
[32m+[m[32m    removable_cache = configs.removable_cache if hasattr(configs, "removable_cache") else False[m
 [m
     if configs.profiled_results:[m
         p_path = configs.profiled_results[m
[36m@@ -648,6 +649,7 @@[m [mdef main(configs):[m
     print(f"merge_prefetch_buffer: {merge_prefetch_buffer}")[m
     print(f"pause_and_resume: {pause_and_resume}")[m
     print(f"static_batching: {static_batching}")[m
[32m+[m[32m    print(f"removable_cache: {removable_cache}")[m
     [m
     if flattened_cache and num_gpu_blocks_override is not None:[m
         num_gpu_blocks_override *= 32 [m
[36m@@ -671,6 +673,7 @@[m [mdef main(configs):[m
         flattened_cache=flattened_cache,[m
         merge_prefetch_buffer=merge_prefetch_buffer,[m
         pause_and_resume=pause_and_resume,[m
[32m+[m[32m        removable_cache=removable_cache,[m
         # static_batching=static_batching,[m
         disable_sliding_window=True,[m
     )[m
[36m@@ -722,6 +725,10 @@[m [mif __name__ == "__main__":[m
                         action="store_true",[m
                         default=False,[m
                         help="whether to use use token deposit")[m
[32m+[m[32m    parser.add_argument("--removable-cache",[m
[32m+[m[32m                        action="store_true",[m
[32m+[m[32m                        default=False,[m
[32m+[m[32m                        help="whether to use use static batching instead of continuous batching")[m
     parser.add_argument("--static-batching",[m
                         action="store_true",[m
                         default=False,[m
[1mdiff --git a/vllm/config.py b/vllm/config.py[m
[1mindex e9d43fec2..d1f2341f4 100644[m
[1m--- a/vllm/config.py[m
[1m+++ b/vllm/config.py[m
[36m@@ -996,6 +996,7 @@[m [mclass CacheConfig:[m
         flattened_cache: bool = False,[m
         pause_and_resume: bool = False,[m
         static_batching: bool = False,[m
[32m+[m[32m        removable_cache: bool = False,[m
         num_layers: int = 32, [m
         need_solver: bool = False,[m
     ) -> None:[m
[36m@@ -1016,6 +1017,7 @@[m [mclass CacheConfig:[m
         self.merge_prefetch_buffer = merge_prefetch_buffer [m
         self.pause_and_resume = pause_and_resume[m
         self.static_batching = static_batching[m
[32m+[m[32m        self.removable_cache = removable_cache[m
         self.gpu_cpu_cache_map: Dict[int, List[int]] = defaultdict(list)[m
 [m
         self._verify_args()[m
[1mdiff --git a/vllm/engine/arg_utils.py b/vllm/engine/arg_utils.py[m
[1mindex 1e26f8f66..21c01fc5f 100644[m
[1m--- a/vllm/engine/arg_utils.py[m
[1m+++ b/vllm/engine/arg_utils.py[m
[36m@@ -206,6 +206,7 @@[m [mclass EngineArgs:[m
     merge_prefetch_buffer: bool = True[m
     pause_and_resume: bool = False[m
     static_batching: bool = False[m
[32m+[m[32m    removable_cache: bool = False[m
     [m
     def __post_init__(self):[m
         if not self.tokenizer:[m
[36m@@ -1084,6 +1085,7 @@[m [mclass EngineArgs:[m
             merge_prefetch_buffer=self.merge_prefetch_buffer,[m
             pause_and_resume=self.pause_and_resume,[m
             static_batching=self.static_batching,[m
[32m+[m[32m            removable_cache=self.removable_cache,[m
         )[m
         parallel_config = ParallelConfig([m
             pipeline_parallel_size=self.pipeline_parallel_size,[m
[1mdiff --git a/vllm/worker/cache_engine.py b/vllm/worker/cache_engine.py[m
[1mindex f132208d5..e88af495c 100644[m
[1m--- a/vllm/worker/cache_engine.py[m
[1m+++ b/vllm/worker/cache_engine.py[m
[36m@@ -612,6 +612,7 @@[m [mclass FlattenedCacheEngine(CacheEngineBase):[m
         self.merge_prefetch_buffer = cache_config.merge_prefetch_buffer     [m
         self.pause_and_resume = cache_config.pause_and_resume[m
         self.static_batching = cache_config.static_batching[m
[32m+[m[32m        self.removable_cache = cache_config.removable_cache[m
         # prefetch_enabled = True [m
         # Initialize the cache.[m
         self.gpu_cache = self._allocate_kv_cache_gpu([m
[36m@@ -1340,65 +1341,58 @@[m [mclass FlattenedCacheEngine(CacheEngineBase):[m
         # NOTE(HONG): ② Removable-cache: dealloc plan following fallback mechanism(pausing) - free extra blocks from paused GPU[m
         missing = 0[m
         if pause_and_resume:[m
[31m-[m
[31m-            # TOTAL_GPU_BLOCKS = self.block_manager.num_total_gpu_blocks[m
[31m-            # HEADROOM_RATIO   = 0.05[m
[31m-            # headroom = max(int(TOTAL_GPU_BLOCKS * HEADROOM_RATIO), HEADROOM_MIN)[m
[31m-            HEADROOM_MIN     = self.block_manager.num_attention_layers * len(snapshot.candidates)            [m
[31m-            headroom = HEADROOM_MIN[m
[31m-            logger.critical(f"[RC] headroom={headroom} (min={HEADROOM_MIN})")[m
[31m-[m
[31m-            free_now = snapshot.free_gpu_blocks + sum(len(v) for v in expected_freed.values())[m
[31m-            logger.critical(f"[RC] free_gpu_blocks={snapshot.free_gpu_blocks}, expected_freed_sum={sum(len(v) for v in expected_freed.values())}")[m
[31m-            alloc_need = sum(len(t[2]) for t in alloc_layers)[m
[31m-            if free_now < alloc_need + headroom:[m
[31m-                missing = alloc_need + headroom - free_now[m
[31m-            logger.critical("[RC] free_now=%d, alloc_need=%d, headroom= %d, missing=%d", free_now, alloc_need, headroom, missing)[m
[31m-[m
[31m-            freed_paused_blks = 0[m
[31m-            if missing > 0:[m
[31m-                logger.critical(f"[RC] Need {missing} additional blocks – scavenging paused seqs")[m
[31m-                # Calculate blocks needed per paused sequence[m
[31m-                num_paused = len(snapshot.paused_gpu_seqs)[m
[31m-                if num_paused > 0:[m
[31m-                    blocks_per_seq = math.ceil(missing / num_paused)[m
[31m-                    logger.critical(f"[RC] Will remove {blocks_per_seq} blocks from each paused sequence(num_paused={num_paused})")[m
[31m-                [m
[31m-                # iterate paused‑GPU seqs in row order – deterministic & fair            \[m
[32m+[m[32m            if self.removable_cache:[m
[32m+[m[32m                # TOTAL_GPU_BLOCKS = self.block_manager.num_total_gpu_blocks[m
[32m+[m[32m                # HEADROOM_RATIO   = 0.05[m
[32m+[m[32m                # headroom = max(int(TOTAL_GPU_BLOCKS * HEADROOM_RATIO), HEADROOM_MIN)[m
[32m+[m[32m                HEADROOM_MIN     = self.block_manager.num_attention_layers * len(snapshot.candidates)[m[41m            [m
[32m+[m[32m                headroom = HEADROOM_MIN[m
[32m+[m[32m                logger.critical(f"[RC] headroom={headroom} (min={HEADROOM_MIN})")[m
[32m+[m
[32m+[m[32m                free_now = snapshot.free_gpu_blocks + sum(len(v) for v in expected_freed.values())[m
[32m+[m[32m                logger.critical(f"[RC] free_gpu_blocks={snapshot.free_gpu_blocks}, expected_freed_sum={sum(len(v) for v in expected_freed.values())}")[m
[32m+[m[32m                alloc_need = sum(len(t[2]) for t in alloc_layers)[m
[32m+[m[32m                if free_now < alloc_need + headroom:[m
[32m+[m[32m                    missing = alloc_need + headroom - free_now[m
[32m+[m[32m                logger.critical("[RC] free_now=%d, alloc_need=%d, headroom= %d, missing=%d", free_now, alloc_need, headroom, missing)[m
[32m+[m
[32m+[m[32m                freed_paused_blks = 0[m
[32m+[m[32m                if missing > 0:[m
[32m+[m[32m                    logger.critical(f"[RC] Need {missing} additional blocks – scavenging paused seqs")[m
[32m+[m[32m                    # Calculate blocks needed per paused sequence[m
[32m+[m[32m                    num_paused = len(snapshot.paused_gpu_seqs)[m
[32m+[m[32m                    if num_paused > 0:[m
[32m+[m[32m                        blocks_per_seq = math.ceil(missing / num_paused)[m
[32m+[m[32m                        logger.critical(f"[RC] Will remove {blocks_per_seq} blocks from each paused sequence(num_paused={num_paused})")[m
[32m+[m[41m                    [m
[32m+[m[32m                    # iterate paused‑GPU seqs in row order – deterministic & fair            \[m
[32m+[m[32m                    for sid in snapshot.paused_gpu_seqs:[m
[32m+[m[32m                        lyr_map = self.mapping.gpu_map.get(sid, {})[m
[32m+[m[32m                        to_offload, freed_ids = self._pick_removable_layers(lyr_map, blocks_per_seq)[m
[32m+[m[32m                        logger.critical("[RC] seq %d: will off‑load layers %s (free %d blocks)",[m
[32m+[m[32m                                    sid, to_offload, len(freed_ids))[m
[32m+[m[32m                        if not to_offload:[m
[32m+[m[32m                            continue[m[41m                    [m
[32m+[m[32m                        # dealloc_layers[sid].extend(to_offload)[m
[32m+[m[32m                        # expected_freed[sid].extend(freed_ids)[m[41m                    [m
[32m+[m[32m                        pause_layers[sid] = to_offload[m
[32m+[m[32m                        missing -= len(freed_ids)[m
[32m+[m[32m                        freed_paused_blks += len(freed_ids)[m
[32m+[m[32m                        logger.critical("[RC] After seq %d → remaining missing=%d", sid, missing)[m
[32m+[m[32m                        if missing <= 0:[m
[32m+[m[32m                            logger.critical("[RC] Target satisfied – stop scavenging")[m
[32m+[m[32m                            logger.critical(f"mapping after scavenging: {self.mapping}")[m
[32m+[m[32m                            break[m
[32m+[m[32m                    if missing > 0:[m
[32m+[m[32m                        logger.critical("[RC] Still short of %d blocks after scavenging paused seqs", missing)[m
[32m+[m[32m            else:[m
[32m+[m[32m                # When removable_cache is false, pause all layers of paused sequences[m
                 for sid in snapshot.paused_gpu_seqs:[m
                     lyr_map = self.mapping.gpu_map.get(sid, {})[m
[31m-                    to_offload, freed_ids = self._pick_removable_layers(lyr_map, blocks_per_seq)[m
[31m-                    logger.critical("[RC] seq %d: will off‑load layers %s (free %d blocks)",[m
[31m-                                sid, to_offload, len(freed_ids))[m
[31m-                    if not to_offload:[m
[31m-                        continue                    [m
[31m-                    # dealloc_layers[sid].extend(to_offload)[m
[31m-                    # expected_freed[sid].extend(freed_ids)                    [m
[31m-                    pause_layers[sid] = to_offload[m
[31m-                    missing -= len(freed_ids)[m
[31m-                    freed_paused_blks += len(freed_ids)[m
[31m-                    logger.critical("[RC] After seq %d → remaining missing=%d", sid, missing)[m
[31m-                    if missing <= 0:[m
[31m-                        logger.critical("[RC] Target satisfied – stop scavenging")[m
[31m-                        logger.critical(f"mapping after scavenging: {self.mapping}")[m
[31m-                        break[m
[31m-                if missing > 0:[m
[31m-                    logger.critical("[RC] Still short of %d blocks after scavenging paused seqs", missing)[m
[31m-[m
[31m-            # TODO(HONG): deprecated -> use the above code and delete this[m
[31m-            # for sid in snapshot.paused_gpu_seqs:[m
[31m-            #     logger.info(f"[PAUSE] Processing seq_id={sid}")[m
[31m-            #     layer_map = self.mapping.gpu_map.get(sid, {})[m
[31m-            #     alive = [lyr for lyr, blks in layer_map.items() if blks]                [m
[31m-            #     if len(alive) <= 1:[m
[31m-            #         # NOTE(HONG): 첫 레이어만 남았거나 이미 비어 있음 → offload 생략[m
[31m-            #         logger.info(f"[PAUSE] seq_id={sid} has one layer left, skipping")                [m
[31m-            #         continue[m
[31m-            #     first_layer = min(alive)              # HACK(HONG): leaving first layer to prevent from being paused_cpu_seqs[m
[31m-            #     layers_to_pause = [lyr for lyr in alive if lyr != first_layer][m
[31m-            #     logger.info(f"[PAUSE] seq_id={sid} → offloading layers except last: {layers_to_pause}")[m
[31m-[m
[31m-            #     pause_layers[sid] = layers_to_pause.copy()                    [m
[32m+[m[32m                    alive_layers = [lyr for lyr, blks in lyr_map.items() if blks][m
[32m+[m[32m                    if alive_layers:[m
[32m+[m[32m                        pause_layers[sid] = alive_layers[m
[32m+[m[32m                        logger.critical("[RC] seq %d: pausing all layers %s", sid, alive_layers)[m
 [m
         # estimate prefetch pages -------------------------------------------------[m
         need_prefetch = self._estimate_prefetch_blocks([m
[1mdiff --git a/vllm/worker/cache_engine_opt.py b/vllm/worker/cache_engine_opt.py[m
[1mindex 3f025325b..a7df5a02c 100644[m
[1m--- a/vllm/worker/cache_engine_opt.py[m
[1m+++ b/vllm/worker/cache_engine_opt.py[m
[36m@@ -93,6 +93,7 @@[m [mclass FlattenedCacheEngine(CacheEngineBase):[m
         self.merge_prefetch_buffer = cache_config.merge_prefetch_buffer     [m
         self.pause_and_resume = cache_config.pause_and_resume[m
         self.static_batching = cache_config.static_batching[m
[32m+[m[32m        self.removable_cache = cache_config.removable_cache[m
         # prefetch_enabled = True [m
         # Initialize the cache.[m
         self.gpu_cache = self._allocate_kv_cache_gpu([m
